//const {  } = require('./oracle-lite.input')

describe('Oracle lite testing suit', () => {
  test('This test it preformed to prevent failure to parse a blank script', () => {
    expect(null).toBeNull()
  })
})